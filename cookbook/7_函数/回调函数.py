# 异步：无法获取返回值，需要callback执行后续处理
# 事件驱动

# 什么是回调函数？
# 旅馆叫醒服务，如果没有回调函数，最多能设置几种方式
# 如果登记了回调函数，只需要给出规范，旅客就能自己定义如何叫醒

# 定义函数
# 调用函数，传入参数（通过回调函数添加额外的用户逻辑：说明函数本身并不完整，回调函数相当于函数的一部分拼图）
# 返回结果

# 因为callback相当于拼图加到函数中，如果是异步，主程序无法获取返回结果，需要callback去后处理

